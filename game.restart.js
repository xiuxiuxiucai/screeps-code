var publicMethod = require('public.method');

/**
 * 诊断当前系统状态并治疗
 * 
 * roomName
 */
var gameRestart = {

    run: function(roomName) {
        // 当前房间
        roomObject = Game.rooms[roomName];
        
        // 检测容器
	}
};

module.exports = gameRestart;